name: Build Hugo site

on:
  push:
    branches: [ master, content ]
  pull_request:
    branches: [ master, content ]
  workflow_dispatch:
    inputs:
      logLevel:
        default: 'warning'

jobs:
  transform:
    runs-on: ubuntu-latest
    steps:
    - name: Inject slug/short variables
      uses: rlespinasse/github-slug-action@v3.x
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v5.3
    - name: List all_changed_files
      run: |
        echo "${{ steps.changed-files.outputs.all_changed_files }}"
    - name: Checkout
      id: checkout
      if: steps.changed-files.outputs.all_changed_files != 'content'
      uses: actions/checkout@master
      with:
        submodules: false
        # Setting true above seems to lead to submodule updation problems.
        fetch-depth: 1
    - name: Update submodules
      if: ${{ github.event_name != 'pull_request'}} && steps.checkout.conclusion == 'success'
      run: |
        set -o xtrace
        git submodule update --init --recursive
        git submodule update --remote --merge --recursive
    - name: Setup Hugo
      if: steps.checkout.conclusion == 'success'
      uses: peaceiris/actions-hugo@v2
      with:
        hugo-version: "0.72.0"
        # extended: true
      # Step 3 - Clean and don't fail
    - name: Clean build_github directory
      if: steps.checkout.conclusion == 'success'
      run: rm -rf build_github
    - name: Build hugo site pages
      if: steps.checkout.conclusion == 'success'
      run: |
        hugo
    - name: Deploy
      if: ${{ github.event_name != 'pull_request'}} and && steps.checkout.conclusion == 'success'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./build_github
        publish_branch: gh-pages